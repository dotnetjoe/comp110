import "introcs";

import {
    SVG,
    Group,
    Rectangle,
    Circle,
    Stroke,
    Color
} from "introcs/graphics";

let svgTag: SVG = new SVG("artboard");
function main(): void {
    let scene: Group = new Group();
    
    // TODO

    svgTag.render(scene);
}

main();