import "introcs";

// Declare a variable named balance that holds a number.
let balance: number;

// Assign a value of 100 to it.
balance = 100;

// Print the balance.
print(balance);

// Subtract 9.99 from balance.
balance = balance - 9.99;

// Print the new balance.
print(balance);