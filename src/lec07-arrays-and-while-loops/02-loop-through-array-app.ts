import "introcs";

function main(): void {

    let a: number[] = [2, 3, 5, 7, 11, 13];

    let i: number = 0;
    while (i < a.length) {
        print(a[i]);
        i = i + 1;
    }
    // TODO:
    // 1. Write a loop while i < a's length
    // 2. Inside the loop:
    //   Print the i'th element of a
    //   Increment i by 1

}

main();