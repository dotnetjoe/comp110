export function main(): void {
    // You should really change style from CSS not TypeScript, but
    // this is a quick demo to convince ourselves the TypeScript code
    // is running!
    document.body.style.background = "black";
    document.body.style.color = "white";
}

main();